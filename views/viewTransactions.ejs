<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #008374; /* Green background */
      color: #000000; /* Black text */
      margin: 0;
      padding: 20px;
    }
    label {
      display: block;
      font-weight: bold;
      font-size: 25px;
      color: #FFFFFF;
    }
    table {
      width: 100%;
      background-color: #FFFFFF; /* White background */
      border: 1px solid #000000; /* Black border */
      border-collapse: collapse;
    }
    th {
      background-color: #00ae99; /* Light green */
      color: #FFFFFF; /* White text */
      text-align: left;
      padding: 8px;
    }
    td {
      padding: 8px;
    }

      a {
    color: #FF6347; /* Golden color for the link */
    text-decoration: none; /* Remove underline by default */
    transition: color 0.3s ease-in-out; /* Smooth color transition */
    font-weight: bold; /* Make the text bold */
  }

  /* Change color on hover */
  a:hover {
    color: #FFD700; /* Change to a slightly different shade on hover */
  }

  /* Change color when active (while clicking) */
  a:active {
    color: #FFD700; /* Change to a different color when clicked */
  }

  /* Styling the anchor tag */
a.arrow-link {
  color: #FFD700; /* Golden color for the link */
  text-decoration: none; /* Remove underline by default */
  transition: color 0.3s ease-in-out; /* Smooth color transition */
  font-weight: bold; /* Make the text bold */
  position: relative; /* Enable positioning of the arrow */
}

/* Styling the arrow */
.arrow {
  margin-right: 5px; /* Adjust the space between text and arrow */
}

/* Creating an arrow using CSS */
.arrow::before {
  content: '\2190'; /* Unicode for left arrow */
  display: inline-block;
  transform: rotate(0deg);
  transition: transform 0.3s ease-in-out; /* Smooth transition for the arrow */
}

/* Rotate arrow on hover */
a.arrow-link:hover .arrow::before {
  transform: rotate(-90deg); /* Rotate the arrow -90 degrees on hover */
}

  </style>
</head>
<body>
   
   <a href="/" class="arrow-link"><span class="arrow">&larr;</span>Go back to home</a>



  <br><br><br>
  <label><b>User Information:</b></label>

  <br>

  <table>
    <thead>
      <tr>
        <th>Type</th>
        <th>Amount</th>
      </tr>
    </thead>
    <tbody>
      <% for (let iCount = 0; iCount < myuser.length; iCount++) { %>
        <tr>
          <td><%= myuser[iCount].transaction_category %></td>
          <td><%= myuser[iCount].amount %></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</body>
</html>
